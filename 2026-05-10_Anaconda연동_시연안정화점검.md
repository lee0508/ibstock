# 2026-05-10 Anaconda연동 시연안정화점검

## 수행 내용
- Anaconda(Miniconda) 설치 경로 확인: `C:\ProgramData\miniconda3`
- conda base 환경에 의존성 설치: `pip install -r backend/requirements.txt`
- 데이터 파이프라인 재검증 및 시연 데이터 50건 복구
- 설정 파싱 보강: `.env`의 `debug=release` 값도 정상 처리되도록 개선

## 코드 변경
- `backend/app/core/config.py`
  - `debug` 필드 문자열 값(`release/prod/on/off` 등) bool 변환 validator 추가

## 데이터 상태(최종)
- raw: 50
- processed: 50
- DB shorts_videos: 50
- FAISS index/metadata/build_info: 존재

## 검증 결과
- `python3 -m compileall backend scripts` -> OK
- `python3 scripts/preflight_check.py` -> `RESULT: OK (ollama warning)`
- Ollama 미기동 상태는 경고로 분리 확인됨

## 2026-05-11 13:00 시연 리스크
1. Ollama 미기동 시 `/api/stock/query` 생성형 답변 대신 fallback 가능
2. YouTube 수집 단계에서 JS runtime/ffmpeg 경고 발생 가능
3. conda 플러그인 오류가 간헐 발생할 수 있어, 필요 시 `--no-plugins` 옵션 사용

## 시연 직전 권장 명령
1. `powershell -ExecutionPolicy Bypass -File .\scripts\run_ollama_local.ps1`
2. `python -X utf8 scripts\preflight_check.py --strict-ollama`
3. `python -X utf8 scripts\verify_local_demo.py`
